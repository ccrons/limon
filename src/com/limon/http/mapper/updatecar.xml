<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.limon.http.dao.UpdateCarDao">
    <update id="updateCar" parameterType="com.limon.http.model.ShoppingCart">
		update app_shoppingcart set productnum=#{count} where productid=#{productid} and storeid=#{storeid} and userid=#{userid}
    </update>
</mapper>
